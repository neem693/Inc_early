<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
    namespace="com.anylogic.iot.api.admin.onm.mapper.StudyMapper">


    <!--  사용자 등록 -->
    <insert id="insertTest2" parameterType="Map">
 		INSERT INTO
        sdp.tb_testinsert(
        	name
        	, age
        	, company
        	, department
        )
        VALUES(
        	#{name}
        	, #{age}::int
        	, #{company}
        	, #{department}
        	)
    </insert>
    
    <select id="selectList" resultType="Map">
        SELECT
            name
            , age
            , company
            , department
        FROM sdp.tb_testinsert
       <!--  where name like CONCAT('%', #{searchTest}, '%') -->
       
    </select>
    
     <select id="searchTest" parameterType="Map" resultType="Map">
        SELECT
            name
            , age
            , company
            , department
        FROM sdp.tb_testinsert
        where name like CONCAT('%', #{searchTest}, '%')
       
    </select>
    
    
    <delete id="deleteOne" parameterType="Map" >
    DELETE 
    FROM sdp.tb_testinsert
    where name = #{name}

    </delete>
    
    <update id="updateOne" parameterType="Map">
    UPDATE
    sdp.tb_testinsert
    set name = #{name},
    	age = #{age}::int,
    	company = #{company},
    	department = #{department}
   	where name = #{search}
    	
    </update>

</mapper>